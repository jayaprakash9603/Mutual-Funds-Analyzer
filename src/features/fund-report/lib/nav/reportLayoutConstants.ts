/** Navbar height (h-16). */
export const REPORT_NAVBAR_HEIGHT_PX = 64

/** Fixed fund-search bar height on the report page. */
export const REPORT_STICKY_BAR_HEIGHT_PX = 52

/** Navbar + report sticky bar — start of scrollable main content. */
export const REPORT_PAGE_TOP_PX = REPORT_NAVBAR_HEIGHT_PX + REPORT_STICKY_BAR_HEIGHT_PX

/** Default offset for section scroll (header + sticky bar + mobile section nav). */
export const REPORT_SECTION_SCROLL_OFFSET = REPORT_PAGE_TOP_PX + 36
